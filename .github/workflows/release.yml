name: Release

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Release is paused during Tauri migration
        run: |
          echo "The legacy Python release workflow is disabled."
          echo "Use the manual Tauri Build workflow for validation until the Tauri app is fully verified."
